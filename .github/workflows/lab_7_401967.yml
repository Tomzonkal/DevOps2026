name: CI

on:
  push:
    branches:
      - lab_7/new_branch_401967

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Upgrade pip
        run: python -m pip install --upgrade pip

      - name: Run tests
        run: |
          cd Lab_7/app_401967
          pip install -r requirements.txt
          python -m pytest test_calculator.py -v