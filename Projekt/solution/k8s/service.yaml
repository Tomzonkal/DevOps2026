apiVersion: v1
kind: Service
metadata:
  name: calculator
  labels:
    app: calculator
spec:
  type: LoadBalancer
  selector:
    app: calculator
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000
